## 250116 补充 
**PS：**
./test2 运行程序之后，输出显示  `---------------输出buffer -------------` 到 `---------------输出buffer end-------------` 之间 即为 RDMA read 读取到的文件信息，即为 RDMA read 成功，程序可以直接 ctrl+c 结束，不用管后面的输出（代码逻辑实现的问题，bug没解决，不影响正常通信）


## 241217  164 -> 162 RDMA read 通信抓包

需要的四组抓包，1kb 30kb 原生beegfs 和 xyp_beegfs 
- 抓包结果文件位于 `/home/greench/Desktop/RESULT/RDMA/4组 抓包   (in 219 host)` 
- client 代码路径为 `/home/oem/xyp/A_Client_on_GPU/read_RDMA_1211_latest （164 server）`
    - 调整 read file size ,修改 `read.cpp` 中 `FhgfsOpsRemoting_readfileVec函数` 中 `size_t toBeRead=30*1024;`
- read 文件位于 `/mnt/beegfs/test_1217/image_data.csv`
- 输出有显示 文件名字 和行号
    - 在 `open.hpp ` 中 添加如下代码，再将 `printf 替换成 LOG_PRINTF` ，`std::cout 替换成 LOG` 
```c
#define LOG_PRINTF(fmt, ...) printf("[%s:%d] " fmt, __FILE__, __LINE__, ##__VA_ARGS__) 
// #define LOG CoutStream日志( __FILE__, __LINE__ )
// #define CoutStream日志(file, line) LOG << "[" << file << ":" << line << "] "
class Logger {
public:
    Logger(const std::string& file, int line) : file_(file), line_(line) {}
    std::ostream& stream() {
        return std::cout << "[" << file_ << ":" << line_ << "] ";
    }
private:
    std::string file_;
    int line_;
};

#define LOG Logger(__FILE__, __LINE__).stream()
```
### 原生 beegfs RDMA

(164) 192.168.5.209 ==> 192.168.5.210 (162) 

162: ens4f0np0 192.168.5.210 mlx5_2 

164: ens5f0np0 192.168.5.209 mlx5_0

```
1KB 抓包
sudo dd if=/mnt/beegfs/test_1217/image_data.csv of=/home/oem/xyp/output/outputfile_$(date +%Y%m%d_%H%M%S)_1KB.csv bs=1024 count=1

162 Server:
sudo tcpdump -i mlx5_2 -w $(date +%Y%m%d_%H%M%S)_read_CPU_beegfs_1KB_RDMA_162Receive.pcap

164 Client:
sudo tcpdump -i mlx5_0 -w $(date +%Y%m%d_%H%M%S)_read_CPU_beegfs_1KB_RDMA_164_Send.pcap
```

```
30KB 抓包
sudo dd if=/mnt/beegfs/test_1217/image_data.csv of=/home/oem/xyp/output/outputfile_$(date +%Y%m%d_%H%M%S)_1KB.csv bs=30720 count=1

162 Server:
sudo tcpdump -i mlx5_2 -w $(date +%Y%m%d_%H%M%S)_read_CPU_beegfs_30KB_RDMA_162Receive.pcap

164 Client:
sudo tcpdump -i mlx5_0 -w $(date +%Y%m%d_%H%M%S)_read_CPU_beegfs_30KB_RDMA_164_Send.pcap
```

### xyp 实现版本的 RDMA 

read 文件位于 /mnt/beegfs/test_1217/image_data.csv
```
1KB 抓包


162 Server:
sudo tcpdump -i mlx5_2 -w $(date +%Y%m%d_%H%M%S)_read_CPU_xyp_1KB_RDMA_162Receive.pcap

164 Client:
sudo tcpdump -i mlx5_0 -w $(date +%Y%m%d_%H%M%S)_read_CPU_xyp_1KB_RDMA_164_Send.pcap
```

```
30KB 抓包

162 Server:
sudo tcpdump -i mlx5_2 -w $(date +%Y%m%d_%H%M%S)_read_CPU_xyp_30KB_RDMA_162Receive.pcap

164 Client:
sudo tcpdump -i mlx5_0 -w $(date +%Y%m%d_%H%M%S)_read_CPU_xyp_30KB_RDMA_164_Send.pcap
```

## 250106 补充原生 beegfs-RDMA 连接建立的通信数据包

read 文件位于 /mnt/beegfs/test_1217/image_data.csv

### 30KB
```
162 server: IP(192.168.5.210)
    EstablishConnection/20250106_104242_read_CPU_beegfs_30KB_RDMA_162Receive_EstablishConnection.pcap
164 Client: IP(192.168.5.209)
    EstablishConnection/20250106_104240_read_CPU_beegfs_30KB_RDMA_164_Send.pcap

标注：
    EstablishConnection/20250106_104242_read_CPU_beegfs_30KB_RDMA_162Receive_EstablishConnection.pcap
    NO.283 ~ NO.285 : RDMA 连接建立
    NO.286 ~ NO.291 ： 48B 长度数据包应该是 鉴权，其他数据包不清楚是什么
    NO.292 : read 请求发出
    NO.294 ： file data 返回
```

### 1KB 
```
同理：
162 server: IP(192.168.5.210)
    EstablishConnection/20250106_103834_read_CPU_beegfs_1KB_RDMA_162Receive_EstablishConnection.pcap
标注：
    NO.166 ~ NO.168 : RDMA 连接建立
    NO.169 ~ NO.174 ： 48B 长度数据包应该是 鉴权，其他数据包不清楚是什么
    NO.175 : read 请求发出
    NO.177 ： file data 返回

```